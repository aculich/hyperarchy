class Java < Thor
  desc 'jar', 'build a jar of all dependencies for jruby and copy it into the lib directory'
  def jar
    system %{
      cd #{File.dirname(__FILE__)}/java;
      mvn assembly:assembly;
      cp target/monarch-1-jar-with-dependencies.jar ../lib/monarch/
    }
  end
end